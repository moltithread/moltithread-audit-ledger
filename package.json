{
  "name": "moltithread-audit-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "audit-ledger": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "node --test",
    "format": "prettier -w .",
    "eval": "recur eval --config evals/recur.yaml || (echo 'Recur not found. See docs/EVALS.md for setup.' && exit 1)"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^22.13.4",
    "eslint": "^9.19.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
